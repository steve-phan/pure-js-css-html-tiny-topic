const x: number = 56;

let y : string = "This is a string"


class hello {
 user? : string = 'hello';
 age? : number = 3;
 constructor(a? : string, b? : number) {
     this.user = a;
     this.age  =b ;
 }
}

const aFunction = ( a : string, b : number ) : any  => {
  console.log(`${a} and the age is ${b} `)
  return false
  
}
aFunction('hello', 10)
aFunction('hello', 10)
aFunction('hello', 10)
aFunction('hello', 10)